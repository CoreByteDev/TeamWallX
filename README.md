Installation steps: 
install the latest version of python 3
1 ⁠pip install pyusb⁠ to handle the Python side.
2 ⁠libusb⁠ installed on the host machine system-wide (like via Homebrew on Mac or ⁠sudo apt install libusb-1.0-0-dev⁠ on Linux).
